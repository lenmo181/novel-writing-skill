# 2026-09-18

## 技能上架包构建（网络小说创作技能 v7.23 → WorkBuddy 技能市场）

- 依据 WorkBuddy 开放平台规范（https://open.workbuddy.cn/docs/skill）完成上架包构建。
- 产出 `dist/网络小说创作技能_v7.23.0_market.zip`：0.21 MB / 38 文件 / 解压 0.46 MB（上限 3MB）。
- 关键改造（全部只发生在构建副本，源目录零改动）：
  - `tools/` → `scripts/`（平台规范脚本目录），同步改写全包 92 处引用；137 处相对引用自检零悬空。
  - 脱敏 9 处 `C:\Users\lenmo\...` → `~/novels/`、`~/.workbuddy/skills/...`；`ZCode` → `WorkBuddy`。
  - SKILL.md frontmatter 补全 `display_name / display_name_en / description_zh / description_en / category / version / author`（author=豫晨）。
  - **剔除 `out/`**：内含 3 份第三方番茄热榜小说正文（《凡骨》《我不是戏神》等）+ 本作者作品《扫地的第三年，我举世无敌》看板截图，版权与首发风险，不进公开包。
- 工具落盘 `.workbuddy/tools/`：`build_market_zip.py`（构建）、`verify_pack.py`（ZIP 结构+脚本自检）、`scan_pack.py`（上架前体检）。
- 提审资料 `dist/上架提交指南.md`（填表文案 + 上传步骤）。
- 待用户操作：开放平台入驻认证 → 上传 ZIP → 提交审核 → 通过后选「公开发布」。

## 垃圾清理（用户确认范围后执行，全部送 Windows 回收站）

- 删除：`tools/__pycache__/`（8 个 .pyc，99.3 KB）、`.workbuddy/tmp/`（构建副本+临时脚本，476 KB）、`.v2c/`、`.video_agent/`（ZCode 插件残留）、`out/` 下 3 份第三方小说正文 + 1 份热榜取证报告（24 KB）。
- 保留并明确告知不建议动：`.git/`（版本历史）、`dist/`（上架包）、`out/` 中 8 张本作者作品看板截图。
- 清理后项目：`tools/` 12 文件 159 KB，`out/` 8 文件 814 KB，`.workbuddy/` 仅 memory + tools（4 个脚本）。
- 工具：`.workbuddy/tools/clean_tmp.py`（回收站删除，SHFileOperationW + FOF_ALLOWUNDO）。

## 上架进度（未完成，等用户登录）

- agent-browser 已打开开放平台并停在登录页（`https://open.workbuddy.cn/login/?platform=open&state=0`），有头窗口（`--headed`）保持开启。
- 阻塞点：登录需用户微信扫码；若未做开发者入驻认证，实名认证（身份证+人脸）必须本人完成，无法代理。

